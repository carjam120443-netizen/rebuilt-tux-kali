# RebuiltTux Kali 1.1 installer profile
# This is configuration data for the future installer ISO build.

PROFILE_NAME=rebuilttux-kali
DISPLAY_NAME="RebuiltTux Kali 1.1"
ARCH=amd64
INSTALLER_TYPE=netinstaller

# Keep the installer connected to the Kali package ecosystem.
# The installer retrieves packages from the configured network mirror.
