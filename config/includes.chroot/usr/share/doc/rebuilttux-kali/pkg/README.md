# RebuiltTux pkg

The `pkg` command is the planned secondary RebuiltTux package manager.

APT remains the primary package manager for Debian/Kali packages. RebuiltTux `pkg` will use its own repository and package metadata.

This command is currently a development scaffold and does not install packages yet.
