# RebuiltTux Kali installer configuration

This directory is reserved for Debian Installer configuration used by the future amd64 network installer.

The installer branding is intentionally separate from the boot branding so the installer can say **RebuiltTux Kali** while the initial boot experience retains a Kali-style layout.

No ISO is generated by this repository yet.
